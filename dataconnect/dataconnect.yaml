specVersion: "v1"
serviceId: "amigosperdidos"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "amigosperdidos-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
